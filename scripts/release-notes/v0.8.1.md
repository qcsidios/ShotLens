## 更新内容

- 修复升级后屏幕录制权限反复失效的问题：发布版改为强制使用稳定 Apple Developer ID Application 证书签名。
- 新增发布签名检查，阻止 ad-hoc 临时签名包进入正式 DMG。
- 本地开发构建仍可使用临时签名，不影响日常调试。

## 注意事项

- 从旧的 ad-hoc 签名版本首次升级到 Developer ID 签名版本时，macOS 可能仍需要重新授权一次屏幕录制。
- 完成这次授权后，只要后续版本继续使用相同 bundle id 和 Team ID，屏幕录制权限不应每次升级都丢失。
