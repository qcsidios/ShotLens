## 更新内容

- 修复 API 测试误报不可用：测试现在以聊天补全端点连通性为准，不再因为模型偶发格式漂移误判。
- 增强翻译稳定性：兼容更多模型返回格式，批量失败后自动修复，仍失败时逐条兜底，减少“API 翻译返回格式无效”。
- 优化控制台 UI：修正检查更新旋转图标轴心，精简重复标题，默认福利说明仅在使用默认福利额度时显示。
- 保留“清空”按钮，并新增“恢复默认”按钮：清空用于完全移除 API 配置，恢复默认用于回到内置福利配置。

## 注意事项

- 默认福利 Key 仍可能限额、失效或被随时撤销，重度用户建议填写自己的 API Key。
- `tencent/Hunyuan-MT-7B` 当前限免，后续可用性和计费以 SiliconFlow/模型服务商政策为准。
