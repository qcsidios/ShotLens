## 更新内容

- 译文严格按 OCR 原位置呈现，不再为了放大文字扩张到相邻区域，减少卡片、菜单和标签栏翻译重叠。
- 修复译文字号适配兜底逻辑，避免小框内容偶发用过大字号绘制。
- 加强翻译质量校验，拦截“被拒绝”“高风险”等模型把网页文字误当安全请求的回答，并自动进入修复/兜底。
- 保留“检测新版本”文字按钮和默认限免文案，继续确保用户自定义 API 设置升级后不丢失。

## 注意事项

- 默认限免 Key 可能限额、失效或被随时撤销，重度用户建议填写自己的 API Key。
- `tencent/Hunyuan-MT-7B` 当前限免，后续可用性和计费以 SiliconFlow/模型服务商政策为准。
