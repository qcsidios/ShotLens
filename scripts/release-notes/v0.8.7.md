## 更新内容

- 将截图捕获切换为 App 内 ScreenCaptureKit 路线，减少触发截图时的可见闪烁。
- 将框选层并入主进程，移除旧的 ShotLensSelect 冷启动 helper，降低全屏 Space 中的窗口切换风险。
- 保留全屏 Space 友好的覆盖窗口层级和非激活面板行为，继续优化全屏浏览器截图翻译体验。
- 取消启动时自动弹出的更新完成提醒；更新检查仍可在控制台手动触发。

## 注意事项

- 本版本继续使用稳定签名，尽量保留 macOS 屏幕录制授权。
- 如果升级后 macOS 仍提示重新授权屏幕录制，请在系统设置中重新勾选 ShotLens。
